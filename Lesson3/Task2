# Task 2
# The valid phone number program.
# Make a program that checks if a string is in the right format for a phone number.
# The program should check that the string contains only numerical characters
# and is only 10 characters long.
# Print a suitable message depending on the outcome of the string evaluation.

phone_number = '6576543879'

if len(phone_number) == 10 and phone_number.isdigit():
    print(f'Your phone number {phone_number} has been recorded successfully!')
elif not phone_number.isdigit():
    print('Use only digits!')
elif len(phone_number) != 10:
    print('Enter exactly 10 digits!')
