# Task 3
# Extracting numbers.
# Make a list that contains all integers from 1 to 100, then find all integers from the list
# that are divisible by 7 but not a multiple of 5, and store them in a separate list.
# Finally, print the list.
# Constraint: use only while loop for iteration

list_len = 100
list100 = range(1, list_len)
list_except = []
i = 0

while i < (list_len-1):
    if list100[i] % 7 == 0 and list100[i] % 5 != 0:
        list_except = list_except + [list100[i]]
    i += 1
print(f'The final list is: {list_except}.')
